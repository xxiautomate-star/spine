# Supabase — persistence for waitlist, memories, api_keys.
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI — used server-side for cloud-mode memory embeddings (1536 dim).
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Anthropic — used by /api/recall for Haiku 4.5 relevance reranking.
# Optional: if unset, recall falls back to fused RRF + decay scoring.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional — used in OG metadata when set.
NEXT_PUBLIC_SITE_URL=https://spine.xxiautomate.com

# Public app URL — used by Stripe Checkout success/cancel redirects and the
# customer portal return URL. Falls back to https://spine.xxiautomate.com.
NEXT_PUBLIC_APP_URL=https://spine.xxiautomate.com

# Stripe — billing. Create Pro ($29/mo) and Power ($99/mo) recurring prices
# in Stripe Test dashboard for Round 14 (switch to live keys in Round 15).
# Webhook endpoint: spine.xxiautomate.com/api/stripe/webhook
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxxxxxxxxxxxxx        # $29/mo Pro
STRIPE_PRICE_ID_POWER=price_xxxxxxxxxxxxxxxxxxxxxxxx      # $99/mo Power/Team

# Schema lives in supabase/schema.sql — apply with:
#   psql "$DATABASE_URL" -f supabase/schema.sql
# or paste into the Supabase SQL editor.
