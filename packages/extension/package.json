{
  "name": "@spine/extension",
  "version": "0.1.0",
  "description": "SPINE browser extension. Captures memories from ChatGPT and Gemini conversations, injects relevant context at the start of new chats.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "package": "node build.mjs && node scripts/zip.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^20.17.17",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
